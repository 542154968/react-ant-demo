.draggle-contain {
    position: relative;

    .col {
        cursor: pointer;
    }

    .dragging {
        position: absolute;
    }

    .attach-contain {
        width: 900px;
        height: 600px;
        margin: 30px auto;
        border: 1px solid #ccc;
    }
}